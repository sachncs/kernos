"""Small, runnable benchmark demos for Kernos.

Each module is a self-contained script that can be invoked via
``python -m examples.benchmarks.<name>``. They use small synthetic or
single-dataset inputs so they finish in seconds on a laptop.
"""

from __future__ import annotations
